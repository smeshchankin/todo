import React from 'react';

const InsertPanel = () => {
    return (
        <div className="add-group">
            <input placeholder="Add item" />
            <button>+</button>
        </div>
    );
};

export default InsertPanel;
